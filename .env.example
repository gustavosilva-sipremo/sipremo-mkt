# Public site URL (no trailing slash) — used for canonical, OG, sitemap
VITE_SITE_URL=https://sipremo.com

# Optional: POST endpoint for contact form (JSON body)
# VITE_CONTACT_API_URL=https://api.example.com/contact
