export type TeamMember = {
  id: string;
  img: string;
};

export type Solution = {
  id: string;
  image: string;
};

export type Testimonial = {
  id: string;
};

export type UseCase = {
  id: string;
  image: string;
};

export type NavItem = {
  id: string;
  href: string;
};

export type LogoItem = {
  id: string;
  icon: string;
};
